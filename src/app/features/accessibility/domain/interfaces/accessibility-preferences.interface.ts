export interface AccessibilityPreferences {
  fontScale: number;
  useAccessibleFont: boolean;
  widgetScaled: boolean;
  lineHeight: number;
  textSpacingLevel: number;
  reducedMotionEnabled: boolean;
  saturationLevel: number;
  contrastLevel: number;
}
